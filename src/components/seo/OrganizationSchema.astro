---
import JsonLd from './JsonLd.astro';
import { siteOrigin } from '~/lib/url';
import { buildOrgGraph } from '~/lib/schema';

const origin = siteOrigin(Astro.site);
const data = buildOrgGraph(origin, 'en');
---
<JsonLd data={data} />

---
/**
 * PaymentBlock — Contact page Block 11.
 *
 * Renders payment terms, currencies, banking details (when payment mode
 * is "public"), and the proforma-invoice WhatsApp CTA. Self-contained.
 */

import en from '~/i18n/en.json';
import { fill } from '~/lib/interpolate';
import { company, waLinkFor } from '~/config/company';

const t = en.contact;
const isBankPublic = company.payment.mode === 'public';
const bank = company.bank;
const piHref = waLinkFor('piRequest');
const paymentWarningBody = fill(t.payment.warningBodyTemplate, {
  director: company.people.owner.name,
});
---
<section aria-labelledby="payment-heading" class="bg-white">
  <div class="mx-auto max-w-7xl px-4 py-16 md:px-8 md:py-24">
    <h2 id="payment-heading" class="text-xl font-bold text-neutral-900 md:text-2xl lg:text-3xl">
      {t.payment.heading}
    </h2>

    <div role="alert" class="mt-6 rounded-xl border-l-4 border-error bg-error/10 p-5">
      <p class="font-semibold text-neutral-900">⚠ {t.payment.warningHeading}</p>
      <p class="mt-2 text-sm text-neutral-800">{paymentWarningBody}</p>
    </div>

    <div class="mt-8 grid gap-8 lg:grid-cols-12">
      <div class="lg:col-span-5">
        <h3 class="text-base font-semibold text-neutral-900">{t.payment.termsHeading}</h3>
        <ul class="mt-3 space-y-2 text-sm text-neutral-700">
          {company.payment.terms.map((term) => (
            <li class="flex gap-2"><span aria-hidden="true" class="text-brand-primary">•</span><span>{term}</span></li>
          ))}
        </ul>
        <h3 class="mt-6 text-base font-semibold text-neutral-900">{t.payment.currenciesHeading}</h3>
        <p class="mt-2 text-sm text-neutral-700">{company.payment.currencies.join(', ')}</p>
        <p class="mt-4 text-sm text-neutral-700">
          <span class="font-medium text-neutral-700">{t.payment.bankCountryLabel}: </span>
          {company.payment.bankCountry}
        </p>
      </div>

      {isBankPublic && (
        <div class="lg:col-span-7">
          <div class="overflow-x-auto rounded-xl border border-neutral-200">
            <table class="w-full text-sm">
              <thead class="bg-neutral-50 text-start">
                <tr>
                  <th class="px-4 py-3 font-semibold text-neutral-700">{t.payment.tableField}</th>
                  <th class="px-4 py-3 font-semibold text-neutral-700">{t.payment.tableValue}</th>
                </tr>
              </thead>
              <tbody class="divide-y divide-neutral-200 bg-white">
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.beneficiary}</td><td class="px-4 py-3 text-neutral-700">{bank.accountName}</td></tr>
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.bankName}</td><td class="px-4 py-3 text-neutral-700">{bank.bankName}</td></tr>
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.bankAddress}</td><td class="px-4 py-3 text-neutral-700">{bank.bankAddress}</td></tr>
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.branch}</td><td class="px-4 py-3 text-neutral-700">{bank.branch}</td></tr>
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.accountNumber}</td><td class="px-4 py-3 text-neutral-700">{bank.accountNumber}</td></tr>
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.swift}</td><td class="px-4 py-3 text-neutral-700">{bank.swiftCode}</td></tr>
                {bank.iban && (
                  <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.iban}</td><td class="px-4 py-3 text-neutral-700">{bank.iban}</td></tr>
                )}
                <tr><td class="px-4 py-3 font-medium text-neutral-900">{t.payment.fields.currency}</td><td class="px-4 py-3 text-neutral-700">{bank.currency}</td></tr>
              </tbody>
            </table>
          </div>
          <p class="mt-3 text-sm text-neutral-600">
            {fill(t.payment.lastVerifiedTemplate, { date: bank.lastVerified })}
          </p>
          <a
            href={piHref}
            target="_blank"
            rel="noopener noreferrer"
            aria-label={t.payment.piButtonAria}
            data-event="whatsapp_click"
            data-source-component="payment-pi"
            class="mt-5 inline-flex items-center justify-center gap-2 rounded-md bg-success px-6 py-3 text-base font-semibold text-white hover:opacity-90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-success"
          >
            {t.payment.piButton}
            <span class="sr-only">{t.newTabSr}</span>
          </a>
        </div>
      )}
    </div>
  </div>
</section>

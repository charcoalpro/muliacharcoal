# Content Audit — /404

**Run date:** 2026-06-23
**Mode:** Diagnose-only (report, no edits)
**Cornerstone:** No (minor supporting page; noindex)
**Passes run:** 0, 1, 2, 3, 5 (Pass 6 OFF)

---

## 1. Manifest (Pass 0)

| Field | Value |
|---|---|
| Target route | `/404` |
| Pillar | Supporting (not part of any pillar-cluster; error/edge page) |
| Source file | `src/pages/404.astro` |
| Layout | `src/layouts/BaseLayout.astro` |
| Components rendered | `Button` (`src/components/cta/Button.astro`) ×2; `WhatsAppButton` (context `contact`); plus layout chrome (`Header`, `Footer`, `SkipLink`, `WhatsAppButton floating` FAB, `Toast`, `Analytics`) |
| Data sources consumed | `~/config/nav` → `footerOperationsNav`; `~/i18n/en` → `en.notFound.*`, `en.cta.backHome`, `en.cta.viewProducts` |
| `company.ts` fields used | None directly. Indirectly via `WhatsAppButton` → `company.whatsapp.e164Digits`, `company.whatsapp.presetMessages.salesGeneral`, `waLink()`; and via layout chrome (Footer/Header) for all company facts |
| Built HTML | `dist/404.html` (Astro special-cases the 404 route to top-level `dist/404.html`, not `dist/404/index.html` — the prompt's expected path is absent by design, not a build error) |
| Schema types emitted | **None.** No `includeOrgSchema`, no `schema` prop passed. `dist/404.html` contains zero JSON-LD. Correct for a `noIndex` error page. |
| Indexability | `noIndex` → built HTML emits `<meta name="robots" content="noindex, nofollow">`. Correct. |

**Build/resolve stop conditions:** None. Page resolves to exactly one source file; build artifact present (read-only inspection only — no build run).

---

## 2. Severity-tiered TODO list

### Blockers
*(honesty-gating violation, hardcoded company fact, fabricated/uncited claim, real orphan/broken pillar link, misplaced FAQPage, build failure, regulatory claim now factually wrong)*

**None.**

Verification notes for the negative findings (so a re-auditor can confirm quickly):
- **No hardcoded company facts.** The page source contains zero literal company values. All copy routes through `en.notFound.*` / `en.cta.*`; the WhatsApp number, preset message, and link are produced by `WhatsAppButton` from `company.ts` (`company.whatsapp.e164Digits`, `waLink()`). The factual values visible in `dist/404.html` (phone `+62 821 287 68 545`, NIB, NPWP, address, directors, capacity, ISO) are all injected by the shared `Footer`/`Header` chrome, not by `404.astro`. Pass.
- **No honesty-gating violation.** The page renders no trust/claim blocks (no certifications, capacity, SP 978, per-order reports). Nothing to gate. Pass.
- **No broken pillar link / orphan.** Every link target on the page (`/`, `/products`, and the 8 `footerOperationsNav` targets: `/products`, `/packaging`, `/logistics`, `/factory`, `/samples`, `/quality`, `/guide`, `/quality/certifications`) is present in `LIVE_ROUTES`. No muted/dead links. A `/404` page is intentionally not in the pillar map and is exempt from the up-link/incoming-link orphan rule (it is reached by server 404 response, not by internal links). Pass.
- **No misplaced FAQPage / wrong schema.** Zero JSON-LD emitted. Correct for noindex. Pass.

### Defects
*(factual Error, missing required schema type, snippet/heading/structure failure, missing Devil's Advocate on cornerstone, missing GEO meta table on cornerstone)*

**None.** This is a minor page, so the absent GEO meta table and absent Devil's Advocate section are Hardening, not Defects (see below). No factual claims are made on the page, so Pass 3 yields no errors. No schema type is required on a noindex error page, so "missing schema" is not a defect here.

### Hardening
*(anti-bloat, missing mini-cases, freshness cadence, connectivity gaps, minor-page omissions)*

| ID | Tier | Pass + rule | Exact location | What is wrong | Recommended fix (described, not executed) |
|---|---|---|---|---|---|
| H1 | Hardening | Pass 1 — content-completeness vs CLAUDE.md "Error and Edge Pages" spec (`/404 — custom 404 page with **search**, popular products, and inquiry CTA`) | `src/pages/404.astro` whole page; built `dist/404.html` `<section>` | The CLAUDE.md spec for `/404` names three elements: search, popular products, inquiry CTA. Popular products (`footerOperationsNav` list under "Popular pages") and inquiry CTA (WhatsApp `context="contact"`) are present; **the search affordance is absent.** Verified there is no site-wide search component anywhere in `src/components`, so this is an unfulfilled spec aspiration, not a regression. Minor-page omission → Hardening. | Either (a) add a lightweight client-side or Pagefind-style search box wired to the static index when a sitewide search ships, or (b) if search is permanently out of scope for this static site, amend the CLAUDE.md "Error and Edge Pages" line so the spec and the page agree. Do not block on this; the popular-pages list is a reasonable substitute. |
| H2 | Hardening | Pass 2 — GEO meta table (minor-page omission per Pass 1 GEO rule) | `src/pages/404.astro`, top of `<section>` | No top-of-page meta table (Author / Reviewed by / Fact-checked / Last updated / Read time). | Correctly omitted — a 404 is not an article and is noindex. No action; documented only to show it was checked and is intended. |
| H3 | Hardening | Pass 2 — Devil's Advocate / mini-cases (minor page) | Whole page | No steelman section and no Problem→Action→Result mini-case. | Correctly absent on an error page; no action. Documented for completeness. |
| H4 | Hardening | Pass 2 — featured-snippet lead / definition-form sentence | `dist/404.html` H1 "Page not found" + lead `<p>` | The page has one H1 and one descriptive paragraph; no definition-form sentence. | Intended for an error page (nothing to define). No action. The single H2 "Popular pages" is a label, appropriately not phrased as a buyer question because this is navigational, not informational, content. |
| H5 | Hardening | Pass 1 — i18n integrity (forward-looking) | `src/pages/404.astro` line 33, `aria-label={en.notFound.popularHeading}` and the `footerOperationsNav` item labels | All user-visible strings route through `en.*` and `nav.ts` (which itself pulls labels from `en.footer.links.*`). Compliant today. The only watch-item: the popular-pages link labels are sourced from `footerOperationsNav`, i.e. the footer's label set; if the footer column is ever re-scoped, the 404's "Popular pages" list changes with it. Not a bug — noted so a future editor knows the coupling is intentional. | No action. Monitor only. |

---

## 3. Claims register (Pass 3)

The page makes **no factual or marketing claims** in its own copy. The only assertions on the page are navigational labels and a generic error message. Facts visible in the built HTML originate from shared chrome (Footer/Header), which is audited under those components' own runs, not here.

| Claim in page | Status | Severity | Correction / comment | Source |
|---|---|---|---|---|
| "The page you were looking for doesn't exist, or it may have moved." (`en.notFound.description`) | Correct | Low | Generic, non-factual error copy. No verification needed. | i18n (`src/i18n/en/common.json`) |
| "Popular pages" list = Products, Packaging, Logistics, Factory, Samples, Quality, Buyer's Guide, Certificates | Correct | Low | All 8 targets are live routes in `LIVE_ROUTES`. Labels match the footer's. | `src/config/nav.ts` `footerOperationsNav` + `LIVE_ROUTES` |
| WhatsApp pre-fill / number (rendered by `WhatsAppButton context="contact"`) | Correct | Low | Number and message come from `company.whatsapp.*` via `waLink()`; not hardcoded on this page. | `src/config/company.ts` |

No claim requires correction.

---

## 4. Requires deep research

**None.** This page asserts no statistic, date, regulatory fact, superlative, or cause-effect claim. Nothing to route to the deep-research companion prompt.

---

## 5. E-E-A-T / HCU assessment (Pass 5)

Scored against the realistic ceiling for a **noindex utility/error page**, not against a cornerstone. An error page's job is fast recovery, not authority — several criteria are N/A-by-design and scored at the "fit for purpose" level rather than penalized.

| Criterion | Score | Justification |
|---|---|---|
| Authorship & expertise | 6/10 | No author by design (utility page). Brand identity, factory facts, and contacts are present via footer chrome, lending baseline trust. |
| Topical authority | 5/10 | Not a topical page; recovers the user toward authoritative pillars (Products, Logistics, Quality, Guide). Appropriate, not deep. |
| Technical health & freshness | 9/10 | Clean noindex/nofollow, correct OG/Twitter, no JSON-LD bloat, preconnect present. (CWV/Lighthouse are DrMax's domain — not re-measured; budgets in CLAUDE.md apply.) |
| Effort | 7/10 | Thoughtful recovery design: primary + secondary buttons, 8 curated popular links, WhatsApp CTA. More than a bare "404." |
| Originality | 5/10 | Standard error-page pattern; nothing novel, nothing needed. |
| Citation quality | N/A→6/10 | No claims, so nothing to cite. Scored neutral-positive because it makes no uncited claims. |
| Freshness / timeliness | 7/10 | Static, evergreen; no date-sensitive content to go stale. The `© 2026` and facts are footer-driven and current. |
| Page intent | 9/10 | Intent (recover a lost visitor and route to conversion) is clear and well served. |
| Structure & readability | 8/10 | Clean heading order (single H1, one H2 label), generous spacing, scannable link list, no walls of text. |
| Mobile | 9/10 | Touch targets ≥44px (`min-h-11`/`min-h-12`), `flex-wrap` link list, centered responsive section. |
| Format-standard adherence | 8/10 | Matches site CTA/button components sitewide; consistent WhatsApp pattern. Minor: deviates from CLAUDE.md 404 spec by omitting search (H1). |
| Trust & spam signals | 9/10 | No spam, no cloaking, noindex honest, real contact/company data in footer, no fabricated claims. |

**PQ (arithmetic mean of 12 scores):** (6+5+9+7+5+6+7+9+8+9+8+9) / 12 = **88 / 12 = 7.33 / 10**

**Verdict:** **Helpful-first.** The page exists to help a lost visitor recover and convert, not to capture search traffic (it is correctly noindex). goodClicks/badClicks prognosis is favorable: a user who lands here is given clear, live exits and a one-tap WhatsApp path, minimizing bounce-to-SERP (badClicks). No search-first/SEO-bait characteristics.

**Lowest-3 criteria and action steps:**
1. **Topical authority (5)** and **Originality (5)** — *Action:* optionally surface 2–3 of the single highest-intent buyer destinations (e.g. a "Request a quote" / inquiry link alongside WhatsApp, and the most-trafficked product category) above the generic popular-pages list, to shorten the path from "lost" to "converting." Low priority — the page already routes to pillars.
2. **Citation quality (6, N/A-driven)** — *Action:* none required; the neutral score reflects absence of claims, which is correct for an error page. No change recommended.
3. **Authorship & expertise (6)** — *Action:* none specific to the page; authority is appropriately delegated to the persistent footer (company legal name, address, certifications). No change recommended for a 404.

Net: the page is fit for purpose. The only actionable item across the whole audit is **H1** (search affordance vs CLAUDE.md spec), and even that is optional pending a sitewide-search decision.

---

*End of report. No files were modified. Awaiting approval before any change is made elsewhere.*

# Blocker Verification — adversarial refute-by-default (2026-06-23)

✅ COMPLETE. Every flagged Blocker was independently re-checked by a separate skeptic agent against the do-not-false-flag rules. "false-flag" means the original Blocker is bogus and should be dropped. **26 blockers → 19 real, 7 false-flag.**

> **Pattern in the false-flags:** 5 of 7 are the rule *"FAQPage emits ONLY at /faq globally"*, which the verifiers found is contradicted by the codebase's documented *"one Q/A = one FAQPage home"* design and by CLAUDE.md's GEO requirement. Correct that rule in `content-audit-page.md`; do not act on those flags.

| Page | Blocker id | Issue | Verdict | Reason |
|---|---|---|---|---|
| / | B1 | "REACH compliant" trust badge renders unconditionally from i18n with no REACH fact anywhere in company.json. First-screen trust assertion with no backing fact = honesty-gating (banking-details severity). | real | Confirmed: badge renders unconditionally in TrustStrip.astro (L30-31, L59-77) from home.json trustStrip.items.reach with NO hasFact gate — unlike the adjacent countries item which IS gated. company.json certifications has only iso9001/imdg/other/patents/halal (no reach key); commercial has no reachCompliant flag. Ships in dist/index.html as "REACH compliant" + "we declare REACH compliance for all coconut-shell binders and starches" — unbacked rendered claim, not honesty-gated omission. |
| / | B2 | Literal "30% T/T deposit" hardcoded in i18n AND contradicts company.commercial.paymentTerms = 50% (company.json L116). Page shows two different deposit figures. | real | Confirmed home.json L342 (faq.items[1].a): literal "30% T/T deposit" typed into i18n, not a token; fill() passes it through verbatim (dist/index.html). Contradicts paymentTerms "50% T/T advance, 50% T/T 3 days before loading" (L116), which the very next FAQ answer renders correctly via {{paymentTerms}} — same page shows both 30% and 50%. |
| / | B3 | Shipping-line set is a company fact (shippingLines, L118) with a ready {{shippingLines}} token; two prose strings hardcode the values literally. | real | Confirmed L118 ["Maersk","MSC","CMA CGM"], token at interpolate.ts L140; TrustStrip label uses the token, but home.json L39 and L346 type "Maersk, MSC and CMA CGM" literally, shipped verbatim. Per CLAUDE.md i18n may hold prose around facts but never the fact values. |
| /about | B1 | i18n hardcodes owner name "Wilson Gosalim" — a company fact as a literal AND factually wrong (canonical owner = Mohamad Sinno). Page shows two contradictory owner names. | real | Confirmed about.json L175 block6.transcriptPlaceholder hardcodes "my name is Wilson Gosalim"; OwnerVideo.astro L65 renders it verbatim. people.json owner = "Mohamad Sinno" (no Wilson Gosalim; nearest is Director Henry Gosalim). dist/about/index.html renders "Wilson Gosalim" once vs "Mohamad Sinno" 11× — two contradictory owner names live. |
| /faq | B1 | A director/owner name written as a literal in i18n instead of the existing executives token; CLAUDE.md forbids names outside company.ts/people.json. | real | Confirmed faq.json L218: literal "Our director Wilson Gosalim is reachable…" with no token; fill() passes verbatim into dist/faq/index.html. An `executives` token exists (interpolate.ts L202) from people.json, whose directors are Henry Gosalim / Gatot Wibowo — "Wilson Gosalim" exists nowhere in the SSoT. |
| /faq | B2 | "Wilson Gosalim" does not exist in people.json; page presents a non-existent person as the director reachable on WhatsApp for KYC, on the answer meant to prove legitimacy. | real | The verify-legitimacy answer (faq.json L218) hardcodes a name absent from the roster (directors Henry Gosalim / Gatot Wibowo; whatsapp-director resolves to Mohamad Sinno). Renders into the visible answer and FAQPage JSON-LD in dist/faq/index.html — a fabricated named verification contact + hardcoded-fact violation. |
| /glossary | B1 | A FAQPage JSON-LD node is emitted on /glossary; rule reserves FAQPage for /faq only. | false-flag | The cited "FAQPage only at /faq" rule is fabricated. Real architecture (faq.astro L11-16, schema/homepage.ts, faqPageGraph.ts, glossaryPage.ts L8-9) is a canonical-home rule: each distinct Q&A set has ONE FAQPage home; many pages emit FAQPage by design. /glossary's FAQPage is an explicitly carved-out, documented exception carrying terminology questions distinct from /faq's buyer-ops questions — no Search Console competition. |
| /glossary | B2 | Hardcodes HS literal 4402.90 in i18n prose, diverging from canonical company.commercial.hsCode = 4402.20 (token hsCode6 exists). | real | Confirmed glossary.json (term hs-code-4402-90, L472-476) types "4402.90" rather than {{hsCode6}}, and glossary.astro L38 token map doesn't include hsCode6. Canonical fact hsCode = "4402.20" (company.json L183); verified findings rate 4402.90 an Error. dist/glossary/index.html renders 4402.90 six times — hardcoded literal contradicting the canonical fact ships to production. |
| /products | B1 | {{auditors}} (ISO 9001 certifier field) is reused to name the issuer of the per-batch COA, collapsing held-cert vs per-order-report; correct field is quality.testing.thirdPartyLabs = [Carsurin, Beckjorindo, SGS]. | real | Verified interpolate.ts:190 {{auditors}} = certifications.iso9001.auditors (["Carsurin","Beckjorindo"]); products.json (grades.intro L73, meaning.business.body L119) + dist/products/index.html use it for "per-batch COA ships with every order". Canonical per-shipment fact is thirdPartyLabs (company.json L1056) incl. SGS — per-order COA bound to wrong field, drops SGS. |
| /products/{shape}-{size}-{grade} | B1 | Intro asserts in past tense that a burn test was conducted and recorded "so you can read the performance as data", but no burn-test data exists — every metric cell pending, curves are alt-text placeholders. | real | grades.ts defines no burnTest for any grade; dist/products/cube-25mm-platinum all 18 stage cells render "—" and curves are ImagePlaceholders. GradeBurnTest.astro L52 renders t.burnTest.intro UNCONDITIONALLY over an empty table — affirmative claim published without its backing fact. |
| /products/{shape}-{size}-{grade} | B2 | Prose asserts two drop-test clips "show the test", but both are "coming soon" placeholders — no video exists. | real | GradeDropTest.astro L39 renders t.dropTest.body ungated ("The two clips below show the test") while grades.ts sets no dropTestVideos; both slots render "coming soon" in dist HTML. Ungated claim contradicted by the placeholder. The numeric "3+" rating is config-backed and not the issue. |
| /logistics/cargo-protection-and-insurance | B1 | "Thermal blanket" + "container desiccant" asserted as facts, but company.json thermalBlanket = null and containerDesiccant = null. Author gated these OUT of KeyFacts yet states them in prose/FAQ/alt. | real | Verified company.json L444/L450 both null. Page gates both out of KeyFacts via === true (astro L39-40; rows absent in dist), yet asserts them unconditionally in #condition prose (L118), FAQ item 3, and figure alt/caption (L119), all in shipped HTML. Positive claims with no backing fact, contradicting the page's own gating. |
| /logistics/cargo-protection-and-insurance | B2 | States absolutely no seller-arranged cover exists and "the factory never insures the cargo", contradicting company.logistics.insurance.basis (CIF seller-arranged cover on request) and the 2026-06-22 locked CIF+CFR decision. | real | company.json L547 incoterms include CFR/CIF; insurance.basis (L626) "Marine insurance can be arranged by the factory on request under CIF". Page (i18n L24/36/60 + dist) says "no seller-arranged cover, so the factory never insures the cargo" with no mention of CIF; basis imported via const L but never rendered. Claim-contradicts-fact; page (dateModified 2026-06-16) predates the update. |
| /logistics/import-to-usa | B1 | A FAQPage node is emitted on a non-/faq route (systemic cocoon emitter across all 7 logistics children). | false-flag | The "FAQPage only at /faq" rule is not the real invariant. The codebase distinguishes cross-cutting buyer-ops Q&As (canonical at /faq) from page-UNIQUE Q&As that emit a self-canonical FAQPage ("one Q/A = one FAQPage home"); pattern shared across logistics/quality/packaging/guide/glossary, and CLAUDE.md mandates FAQPage on cluster pages. dist/logistics/import-to-usa carries one FAQPage of six US-specific questions not duplicated on /faq. |
| /factory | B1 | City "Semarang" (= company.location.city) hardcoded in i18n instead of the {{city}} token used elsewhere in the same file. | real | company.json L32 city = "Semarang"; token at interpolate.ts L104; factory.json L8 hardcodes "Semarang" in the H1; dist/factory/index.html renders "…Factory in Semarang" with no interpolation. Same file uses {{city}} 10+ times — a literal fact value in i18n, forbidden by CLAUDE.md. |
| /factory | B2 | Country "Indonesia" (= company.location.country) hardcoded in i18n; a {{country}} token exists. | false-flag | "Indonesia" as a keyword-bearing prose word in a title is an accepted repo-wide editorial convention, not the managed fact value: the homepage title (home.json:3) also writes "Indonesia" literally while tokenizing {{brand}}/{{city}}; literal appears unflagged in ~30 i18n locations. {{country}} is reserved for the structured address line. Flagging only this one of many identical instances is not the operative standard. |
| /factory/capacity | B1 | Spelled-out "Four" is the production-line count (company.production.lines = 4); every other mention uses {{productionLines}}; caption hardcodes it and will drift. | real | factoryCapacity.json L7 figure.caption = "Four production lines"; capacity.astro L129 passes it WITHOUT fill() (unlike adjacent alt={fill(...)}). Source is company.production.lines = 4 (L190), token at interpolate.ts L146. dist/factory/capacity shows figcaption "Four production lines" while every other mention renders "4". |
| /quality/specifications-explained | B1 | The three ash-band values are written as literals in i18n instead of from quality.ashGradingFramework.tiers; a token exists only for the Platinum band. | real | qualitySpecs.json L88 types "Platinum (1.6–1.9%), Super Premium (1.9–2.3%), Premium (2.3–2.5+%)", rendered verbatim in dist. Canonical facts in ashGradingFramework.tiers (company.json L1044-1046); the only band token resolves Platinum alone — Super Premium and Premium are not tokenized. Hardcoded fact values in i18n. |
| /quality/testing-methods | B1 | ISO 1171 is a coal/coke ash standard; project's own research says coal/coke standards must NOT be substituted for charcoal (correct: ASTM D1762-84(2021) or ISO 18122). | real | company.json L1051 ashMethod = "ISO 1171:2024", rendered in dist meta description, KeyFactsBox, Ash "Standard:" line, FAQ. guide-research-findings.md L77 + live guide.json (L297, L429) state ISO 1171 is a coal/coke standard not to be substituted; charcoal-correct is ISO 18122:2022 / ASTM D1762. Genuine factual error. |
| /quality/testing-methods | B2 | ISO 1928 is a coal/coke gross-calorific standard (wrong class); biofuel-correct is ISO 18125:2017. Also cited with no edition year, contradicting company.ts's documented intent. | real | company.json calorificMethod = "ISO 1928" renders literally (dist meta, KeyFactsBox, Calorific "Standard" line, FAQ + JSON-LD). guide-research-findings.md L73 names ISO 18125:2017 as charcoal-correct, L77 forbids coal/coke substitution. Contradicts company.ts comment (L488-491) that calorificMethod "renders generically". |
| /quality/certifications | B1 | Shared builder emits a full FAQPage node (4 Q/A) on this cluster page; rule says FAQPage only at /faq. | false-flag | The "FAQPage only at /faq" rule is false for this codebase: dist shows FAQPage on 27 pages by deliberate design (one Q/A = one FAQPage home). qualityClusterPage.ts L77-82 emits COA-specific Q&A; CLAUDE.md + the audit manifest designate /quality/certifications as the canonical COA FAQ home. Policy is "schema follows the canonical content home" — intended, not a defect. |
| /packaging | B1 | Page emits a FAQPage node on /packaging; rule restricts to /faq only. | false-flag | Contradicts the actual architecture: FAQPage is a deliberate sitewide pattern on 27 built pages via 16 schema builders; packagingHub.ts L8-11 + index.astro L10-13 specify FAQPage while FAQSection.astro is suppressed to avoid a duplicate. CLAUDE.md GEO rules mandate FAQPage on FAQ pages; visible answers exist in body HTML regardless. |
| /packaging | B2 | Origin-country "Indonesia" (= company.address.country) is a literal in i18n instead of {{country}} (in scope at the render site). | real | "Made in Indonesia" typed verbatim at packaging.json:222; {{country}} token (interpolate.ts:106) is in scope (body2 rendered via fill(..., tokens) with companyTokens spread). "Made in {{country}}" would render identically; literal ships in dist/packaging. The same sentences already tokenize {{unNumber}}/{{unClass}}; no marking-phrase exemption applies. |
| /packaging/inner-box | B1 | Prose/FAQ assert "tuck-end, two-piece tray-and-lid, and sleeve" box styles unconditionally, but company.json packaging.innerBox.boxStyles = [] (structured row correctly omits). | real | boxStyles = [] → hasFact false; page correctly omits the KeyFacts row + stylesLineTemplate (astro:73, 223-225; absent in dist) — yet construction.p1 and the "What box styles are available?" FAQ assert that exact capability unconditionally (both in built HTML). Specific capability claim with no backing fact, bypassing the page's own gate. |
| /packaging/inner-box | B2 | Prose/FAQ assert emboss/UV-spot/foil finish ladder and "priced separately" while finishes = [] and surcharge fields empty. | false-flag | The gate the page enforces is the price-NUMBER gate ("Available on request", no figure) and the enumerated-list gate (Finishes row + finishesLineTemplate omitted, confirmed in dist). "Priced separately" is a billing-structure line mandated by build-prompt [A2] (no double-count), renders no price, and the coating→lamination→emboss/UV/foil ladder is a locked editorial taxonomy (finishes are a P0 data gap under graceful degradation; identical line appears on /packaging/white-label). Not a fabricated fact behind an honored gate. |
| /guide/how-to-choose-shisha-charcoal-factory | B-01 | A FAQPage node is emitted on this guide page; global rule permits FAQPage only at /faq. | false-flag | The "FAQPage only at /faq" rule does not exist in this project and is contradicted by four authorities: CLAUDE.md GEO requires FAQPage on cornerstone guides; this page's build prompt (lines 38, 50) lists FAQPage as a required deliverable; site-wide builders all emit page-specific FAQPage ("one Q/A = one FAQPage home"); canonicalization-audit-report.md contains no such rule. guideArticlePage.ts:110-112 emission (FAQPage @id = pageUrl#faq via Article mainEntity) is the intended pattern. |
